@namespace TwBlazor.Components
@using TwBlazor.Enums
@inherits TwBlazorComponentBase

@if (IsParent)
{
    <button id="@Id"
        type="button"
        @onclick="OnClick"
        aria-controls="@ChildrenId"
        aria-expanded="@(IsCollapsed ? "false" : "true")"
        class="@parentClasses" style="@Style" @attributes="Attributes" aria-label="@AriaLabel" aria-labelledby="@AriaLabelledBy">
        <span class="@theme.NavigationItemToggleLabel">@Label</span>
        <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" fill="currentColor" class="@toggleIconClasses" aria-hidden="true">
            <path fill-rule="evenodd" d="M5.22 8.22a.75.75 0 0 1 1.06 0L10 11.94l3.72-3.72a.75.75 0 1 1 1.06 1.06l-4.25 4.25a.75.75 0 0 1-1.06 0L5.22 9.28a.75.75 0 0 1 0-1.06Z" clip-rule="evenodd" />
        </svg>
    </button>
}
else
{
    <a id="@Id" href="@Href" class="@classes" style="@Style" @attributes="Attributes" aria-label="@AriaLabel" aria-labelledby="@AriaLabelledBy" aria-current="@(isActive ? "page" : null)">
        <span>@Label</span> 
        @if (NavigationItem?.New ?? false)
        {
           <TwChip Color="Color.Info" AriaLabel="New component" Variant="ButtonVariant.Outlined" Label="New" Size="ChipSize.Small" Class="@($"h-5 {options.Theme.Spacing.PushEnd}")" />
        }
    </a>
}