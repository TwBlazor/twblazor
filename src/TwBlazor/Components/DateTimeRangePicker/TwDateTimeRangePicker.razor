@namespace TwBlazor.Components
@using TwBlazor.Components.DatePicker
@using TwBlazor.Components.TimePicker
@inherits TwPopoverPickerComponentBase

<TwInputRoot @ref="@InputRoot" RootId="@RootId" RootClass="@classes" RootAttributes="@RootAttributes" RootStyle="@Style" RootAriaLabel="@AriaLabel" RootAriaLabelledBy="@AriaLabelledBy">
    <TwInputLabel Id="@LabelId" For="@Id" Label="@Label" Class="@LabelClass" @attributes="@LabelAttributes" />
    <div class="relative">
        <div class="absolute top-0 start-0 h-10.5 flex items-center ps-3 cursor-pointer" role="button" tabindex="@(Disabled ? "-1" : "0")" aria-label="Open datetime range picker" aria-disabled="@(Disabled ? "true" : null)" @onclick="OnIconClickAsync" @onkeydown="OnIconKeyDownAsync">
            <svg class="text-gray-500 dark:text-gray-400 h-5 w-5" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" viewBox="0 0 24 24">
                <path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 10h16m-8-3V4M7 7V4m10 3V4M5 20h14a1 1 0 0 0 1-1V7a1 1 0 0 0-1-1H5a1 1 0 0 0-1 1v12a1 1 0 0 0 1 1Zm3-7h.01v.01H8V13Zm4 0h.01v.01H12V13Zm4 0h.01v.01H16V13Zm-8 4h.01v.01H8V17Zm4 0h.01v.01H12V17Zm4 0h.01v.01H16V17Z" />
            </svg>
        </div>

        <TwTextfield
            @ref="@trigger"
            T="string"
            Id="@Id"
            InputType="text"
            Disabled="@Disabled"
            OnFocus="OnFocusAsync"
            Value="@Value"
            ValueChanged="@((e) => OnTextValueChanged(e))"
            ReadOnly="@ReadOnly"
            Variant="@Variant"
            Invalid="@Invalid"
            ErrorMessage="@ErrorMessage"
            Attributes="@triggerAttributes"
            Class="@textfieldClasses"
            Placeholder="@effectivePlaceholder" />
    </div>

    @if (isFocused)
    {
        <div class="@datepickerContainerClasses" role="dialog" aria-label="@(stage == DateTimeRangePickerStage.Start ? "Choose start date and time" : "Choose end date and time")" tabindex="-1" @ref="PanelRef" @onkeydown="OnPanelKeyDownAsync">
            <div class="w-67">
                <div class="@stageTabsClasses" role="tablist" aria-label="Range step">
                    <button type="button" role="tab" class="@GetStageTabClasses(DateTimeRangePickerStage.Start)" aria-selected="@(stage == DateTimeRangePickerStage.Start ? "true" : "false")" @onclick="@(() => SwitchStage(DateTimeRangePickerStage.Start))">
                        Start
                    </button>
                    <button type="button" role="tab" class="@GetStageTabClasses(DateTimeRangePickerStage.End)" aria-selected="@(stage == DateTimeRangePickerStage.End ? "true" : "false")" @onclick="@(() => SwitchStage(DateTimeRangePickerStage.End))">
                        End
                    </button>
                </div>
                <TwDatePickerCalendar AnchorDate="anchorDate" AnchorDateChanged="@((d) => anchorDate = d)" View="view" ViewChanged="@((v) => view = v)" SelectedDate="stageDate" Range="SelectedRange" MinDate="@effectiveMinDate" MaxDate="MaxDate" PreserveAnchorDay="false" ShowTodayIndicator="false" DaySelected="SelectDateAsync" Navigated="@(() => pendingViewFocus = true)" />
                @if (view == DatePickerCalendarView.Day)
                {
                    <TwDatePickerBody Class="time">
                        <TwTimePickerBody Id="@Id" Is12HourFormat="@Is12HourFormat" SelectedTime="@pendingTime" SelectedTimeChanged="@OnPendingTimeChanged" />
                    </TwDatePickerBody>
                }
            </div>
        </div>
    }
</TwInputRoot>
